services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  fusion:
    build: .
    volumes:
      - ./workspace:/workspace
    depends_on:
      - redis
    env_file:
      - ./workspace/.env
    command: ["tail", "-f", "/dev/null"]
