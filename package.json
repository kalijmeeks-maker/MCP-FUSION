{
  "name": "mcp-fusion",
  "version": "0.1.0",
  "private": true,
  "description": "MCP-FUSION - Model Context Protocol fusion layer",
  "scripts": {
    "start": "./run_fusion.sh start",
    "stop": "./run_fusion.sh stop",
    "reload": "./run_fusion.sh reload",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "gemini:sync": "./scripts/gemini_sync.sh",
    "format": "prettier --write .",
    "lint": "echo \"No linter configured. Add one to package.json.\""
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
